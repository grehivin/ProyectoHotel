@model Entidades.Usuarios

@{
    ViewData["Title"] = "Usuarios";
}

<h1>Información</h1>

<div>
    <h4>Usuario</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Usuario)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Usuario)
        </dd>
        <!--
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Contrasena)
        </dt>
        <dd class="col-sm-10" hidden>
            @Html.DisplayFor(model => model.Contrasena)
        </dd>
        -->
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.UsuarioActivo)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.UsuarioActivo)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Usuario" class="btn btn-warning">Editar</a> 
    <a asp-action="Index" class="btn btn-dark">Cerrar</a>
</div>
