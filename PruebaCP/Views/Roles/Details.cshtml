@model Entidades.Roles

@{
    ViewData["Title"] = "Roles";
}

<h1>Información</h1>

<div>
    <h4>Rol</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Descripcion)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Descripcion)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.RolActivo)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.RolActivo)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.IdRol" class="btn btn-warning">Editar</a> 
    <a asp-action="Index" class="btn btn-dark">Cerrar</a>
</div>
